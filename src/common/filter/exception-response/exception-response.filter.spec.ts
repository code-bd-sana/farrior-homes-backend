import { ExceptionResponseFilter } from './exception-response.filter';

describe('ExceptionResponseFilter', () => {
  it('should be defined', () => {
    expect(new ExceptionResponseFilter()).toBeDefined();
  });
});
